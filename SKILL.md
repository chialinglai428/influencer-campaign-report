---
name: influencer-campaign-report
description: 網紅專案執行結案數據整理 skill。把多位網紅的 IG/FB 貼文洞察報告或 Reel 洞察報告截圖,轉換成一份加總與加權平均互動率的成效總表(xlsx)。當使用者說「幫我整理網紅成效」「這是XX的後台截圖,粉絲數是XX」「網紅專案結案報表」「幫我加總這些網紅的數據」「貼文洞察報告」「Reel洞察報告」,或提供社群後台截圖並要求整理成表格時,務必啟動此 skill。輸入可以是純截圖、純手動輸入的文字數字,或兩者混合(例如貼一張 profile 截圖抓粉絲數,另一張後台截圖抓互動數據)。
---

# 網紅專案結案數據整理

把多位網紅的社群後台截圖(或手動提供的數字)整理成一份總表,含加總與加權平均互動率。這是網紅專案執行完畢後的結案報表流程。

## 輸入方式(三選一,都可混用)

1. **純截圖**:使用者貼「貼文洞察報告」或「Reel 洞察報告」截圖,直接讀圖辨識文字數字
2. **純手動輸入**:使用者直接打字告訴各欄位數值
3. **雙截圖模式**:一張是網紅的 profile 頁面截圖(用來讀粉絲數),另一張是後台洞察報告截圖(用來讀互動數據),兩張都讀完再合併成一列資料

無論哪種方式,每新增一位網紅,先把讀到/被告知的數字覆誦給使用者確認一次,再寫入表格。

## 需要蒐集的欄位

| 欄位 | 是否必填 | 說明 |
|---|---|---|
| 網紅名稱 | 必填 | |
| 粉絲數 | 必填 | 從 profile 截圖、後台摘要卡片,或使用者直接告知 |
| 觸及帳號數 | 必填 | 見下方「觸及數判斷優先序」 |
| 愛心數 | 必填 | 貼文上方的愛心圖示數字 |
| 留言數 | 必填 | 對話框圖示數字 |
| 轉發數 | 必填 | = 迴轉箭頭圖示(轉發/Repost)+ 紙飛機圖示(傳送/Send)兩者相加 |
| 收藏數 | 必填 | 書籤圖示數字 |
| 連結點擊數 | **Optional** | 使用者可略過不提供;若略過則該欄留空,不計入加總 |

## 觸及帳號數判斷優先序

不同版型(IG 貼文 / Reel / 英文介面 / 協作貼文)顯示的分母標籤不一致,依下列優先序選用第一個出現的欄位當「觸及帳號數」:

1. 「觸及的帳號數量」/「接觸的帳戶數量」/ Reach / Viewers(摘要卡片中)
2. 「瀏覽人數」(部分後台版本用這個詞取代觸及)
3. 「觀看次數」(若前兩者都沒有,退而求其次用觀看次數當估算值,並跟使用者說明這是用觀看數估算,非精準觸及數)

若使用者明確指示「把 XX 的觀看數當觸及數」,以使用者指示為準。

## 互動率公式

互動率 = (愛心數 + 留言數 + 轉發數) ÷ 觸及帳號數

**務必用 Excel 公式寫入儲存格,不要在 Python 裡先算好數字再寫死。**

## 加總邏輯

- 粉絲數、觸及帳號數、愛心數、留言數、轉發數、收藏數、連結點擊數:直接 SUM 加總
- 平均互動率:加權平均,以觸及帳號數加權,公式為
  `=SUMPRODUCT(愛心欄:愛心欄+留言欄:留言欄+轉發欄:轉發欄)/總觸及帳號數`
  (不要用簡單平均,因為不同網紅觸及量差異大,簡單平均會失真)

## 產出報表

使用 `scripts/build_report.py` 的 `build()` 函式產生 xlsx:

```python
import sys
sys.path.insert(0, '/mnt/skills/user/influencer-campaign-report/scripts')  # 或實際skill路徑
from build_report import build

data_rows = [
    {"name": "網紅A", "followers": 15000, "reach": 10104, "likes": 1174,
     "comments": 13, "shares": 245, "saves": 185, "link_clicks": 134},
    {"name": "網紅B", "followers": 18000, "reach": 7262, "likes": 260,
     "comments": 10, "shares": 22, "saves": 13, "link_clicks": None},  # 略過連結點擊
]

build(data_rows, "/home/claude/influencer_report.xlsx")
```

產出後**務必**執行 recalc 驗證公式無誤:

```bash
python /mnt/skills/public/xlsx/scripts/recalc.py influencer_report.xlsx
```

確認 JSON 回傳 `"status": "success"` 且 `total_errors: 0` 才算完成,若有錯誤要修正後重跑。

最後複製到 `/mnt/user-data/outputs/` 並用 present_files 分享給使用者。

## 報表欄位順序(與現有框架一致,勿更動)

網紅名稱 | 粉絲數 | 觸及帳號數 | 愛心數 | 留言數 | 轉發數(轉發+傳送) | 收藏數 | 互動率 | 連結點擊數

樣式:標題列深紫底白字粗體(4B0082),總計列淺紫底(E6E0F8)粗體,字型 Arial,互動率格式為 0.0%。

## 每新增一位網紅後的回報格式

新增完一位,用表格列出該筆資料(對照上表欄位),接著更新目前累計的總計數字(粉絲數、觸及、愛心、留言、轉發、收藏、加權平均互動率、連結點擊數)。若有任何資料來源標籤跟別人不一致(例如用瀏覽人數代替觸及數),直接說明差異,不需要修飾語氣。
